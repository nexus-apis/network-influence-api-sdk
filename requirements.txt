# Dependencias Python — Network Influence API

## Core científico / numérico

```
numpy==1.26.4
scipy==1.13.0
pandas==2.2.2
scikit-learn==1.4.2
statsmodels==0.14.2
```

## Grafos y análisis de redes

```
networkx==3.3
```

## Inferencia causal

```
pgmpy==0.1.26
causal-learn==0.1.3.8
dowhy==0.11.1
```

## Backend API

```
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9
httpx==0.27.0
```

## Cola de tareas / workers

```
celery==5.4.0
redis==5.0.4
kombu==5.3.7
flower==2.0.1
```

## Base de datos relacional

```
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
```

## Base de datos de grafos (Neo4j)

```
neo4j==5.20.0
py2neo==2021.2.4
```

## Bootstrap / estadística avanzada

```
arch==6.3.0
pingouin==0.5.4
```

## Serialización / exportación de grafos

```
lxml==5.2.1
rdflib==7.0.0
pydot==2.0.0
graphviz==0.20.3
```

## Seguridad / autenticación

```
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
```

## Observabilidad / logging

```
structlog==24.1.0
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
prometheus-client==0.20.0
sentry-sdk[fastapi]==2.2.0
```

## Testing

```
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
pytest-mock==3.14.0
hypothesis==6.100.1
factory-boy==3.3.0
```

## Calidad de código

```
ruff==0.4.4
mypy==1.10.0
black==24.4.2
pre-commit==3.7.1
```

## Utilidades generales

```
tenacity==8.3.0
python-dotenv==1.0.1
orjson==3.10.3
ujson==5.10.0
click==8.1.7
rich==13.7.1
tqdm==4.66.4
joblib==1.4.2
```

## Notas de compatibilidad crítica

```
# causal-learn requiere numpy<2.0 → numpy==1.26.4 es el techo seguro
# pgmpy==0.1.26 requiere torch opcional; instalar separado si se usa BayesianNetwork GPU
# py2neo es legacy pero estable para Neo4j 5.x en modo read; neo4j driver oficial para writes
# arch==6.3.0 cubre GARCH + pruebas de causalidad Granger multivariada que statsmodels no expone
# uvicorn[standard] incluye watchfiles + websockets necesarios para webhooks de JobOrchestrator
```